# Mysql Docker Compose

Simple docker for local testing and development

## Build and run 

```shell 
docker compose up --build -d
```

## Run only

```shell 
docker compose up -d
```

## stop 
```shell
docker-compose stop
```